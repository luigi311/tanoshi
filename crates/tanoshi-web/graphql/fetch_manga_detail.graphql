query FetchMangaDetail($id: Int, $refresh: Boolean) {
  manga(id: $id, refresh: $refresh) {
    id
    title
    author
    genre
    coverUrl
    description
    status
    isFavorite
    source {
      name
    }
    chapters(refresh: $refresh) {
      id
      title
      scanlator
      uploaded
      readAt
      lastPageRead
    }
  }
}